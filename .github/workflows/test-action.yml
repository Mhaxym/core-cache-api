name: Testing Action
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name of user'
        required: true
        default: 'Maxhym'
      home:
        description: 'Location of user'
        required: false
        default: 'Mallorca'

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "This is $!"
        echo "- and $!"
